import express from 'express';
import { z } from "zod";

const router = express.Router();

router.post('./create', async(request, response, next) => {
    try{
        const parseVar =  ZUserAuthInput.parse(a)
    }
    catch{

    }
});

export default router;