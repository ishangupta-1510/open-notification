import { AppInterfaces } from "../../interfaces";
import { Express } from "express";

export class User implements AppInterfaces.User {
    async hello(input: any): Promise<any> {
        const prisma
        const varA = await prisma.tableA.create({
            data: {

                
            }
        })
    }
}